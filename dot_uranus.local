case "$OSTYPE" in
  linux*)
    ;;
  openbsd*)
    export XDG_RUNTIME_DIR=/tmp/run/user/$UID
    unalias reboot
    reboot() { shutdown -r now ; }
    unalias halt
    halt() { shutdown -h now ; }
    ;;
  *)
    ;;
esac

