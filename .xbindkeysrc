# ~/.xbindkeysrc
# Version: 1.8.7
#
# To specify a key, you can use 'xbindkeys --key' or
# 'xbindkeys --multikey' and put one of the two lines in this file.
#
# The format of a command line is:
#    "command to start"
#       associated key
#
# A list of keys is in /usr/include/X11/keysym.h and in
# /usr/include/X11/keysymdef.h
# The XK_ is not needed.
#
# List of modifier:
#   Release, Control, Shift, Mod1 (Alt), Mod2 (NumLock),
#   Mod3 (CapsLock), Mod4, Mod5 (Scroll).
#
# The release modifier is not a standard X modifier, but you can
# use it if you want to catch release events instead of press events
#
# By defaults, xbindkeys does not pay attention with the modifiers
# NumLock, CapsLock and ScrollLock.
# Uncomment the lines above if you want to pay attention to them.
#
#keystate_numlock = enable
#keystate_capslock = enable
#keystate_scrolllock= enable

# Title: Launch default terminal
"$HOME/bin/term"
  Mod4 + Return

# Title: Rofi - Program Launcher
"rofi -combi-modi drun,run -modi combi -show combi -display-combi launch"
  Mod4 + p

# Title: Lock Screen
"sleep .5;touch /run/user/1000/.screenlocker_force;echo alt+shift+l>/run/user/1000/.screenlocker_method;xset s activate"
  Alt + Shift + l

# Title: Lock Screen
"sleep .5;touch /run/user/1000/.screenlocker_force;echo alt+shift+s>/run/user/1000/.screenlocker_method;xset s activate"
  Alt + Shift + s

# Title: Close all notifications
"/usr/bin/dunstctl close-all"
  Control+Mod2 + space

# Title: Cheatsheet - Qutebrowser
"/usr/bin/feh $HOME/.config/qutebrowser/cheatsheet.png"
  Alt + Shift + w

# Title: Cheatsheet - XBindKeys
"IFS=$(printf '\n\b');unset next skip;for line in $(cat ~/.xbindkeysrc); do [ $next ] && unset next && echo $(echo $line|sed -r 's/^ +//;s/Mod2 \+ //'): $title; [ $skip ] && unset skip && next=1; echo $line|grep -q '^# Title: ' && title=$(echo $line | sed 's/^# Title: //') && skip=1; done | sort -t: -k2 | column -s: -t | yad --text-info --height 780 --width 700"
  Alt + Shift + x

# Title: Rofi - Phonetic Alphabet
"cat $HOME/.notes/alpha | rofi -dmenu -p 'phonetic alphabet'"
  Alt + Shift + a

# Title: Rofi - Linkding Bookmarks
"rofi -show linkding -modi linkding:rofi-linkding"
  Alt + Shift + k

# Title: Rofi - Calculator
"rofi -show calc -modi calc -no-show-match -no-sort"
  Alt + Shift + c

# Title: Rofi - Power Menu
"rofi -show p -modi p:~/bin/rofi-power-menu"
  Alt + Shift + p

# Title: Rofi - Power Menu
"rofi -show p -modi p:~/bin/rofi-power-menu"
  Control + Alt + Delete

# Title: Screenshot - GUI
"flameshot gui"
  Mod4 + Shift + s

# Title: Screenshot - GUI
"flameshot gui"
  Print

# Title: Screenshot - Current screen to clipboard
"flameshot screen -c"
  Alt + Print

# Title: Screenshot - Current screen to ~/Pictures
"flameshot screen -p ~/Pictures"
  Alt + Shift + Print

# Title: Screenshot - Full desktop to clipboard
"flameshot full -c"
  Control + Print

# Title: Screenshot - Full desktop to ~/Pictures
"flameshot full -p ~/Pictures"
  Mod4 + Print

# Title: Rofi - Emoji Picker
"rofi -modi 'emoji:/usr/bin/rofimoji' -show emoji"
  Alt + Shift + e

# Title: Rofi - Bitwarden
"$HOME/bin/bwmenu --auto-lock 7200"
  Alt + Shift + b

# Title: Screen Brightness - Increase
"$HOME/bin/adjust_brightness up"
  Mod2 + XF86MonBrightnessUp

# Title: Screen Brightness - Increase
"$HOME/bin/adjust_brightness up"
  Mod4 + Up

# Title: Screen Brightness - Decrease
"$HOME/bin/adjust_brightness down"
  Mod2 + XF86MonBrightnessDown

# Title: Screen Brightness - Decrease
"$HOME/bin/adjust_brightness down"
  Mod4 + Down

# Title: Speaker Volume - Mute
"pactl set-sink-mute @DEFAULT_SINK@ toggle;pkill -RTMIN+11 dwmblocks"
  Mod2 + XF86AudioMute

# Title: Speaker Volume - Mute
"pactl set-sink-mute @DEFAULT_SINK@ toggle;pkill -RTMIN+11 dwmblocks"
  Mod4 + m

# Title: Speaker Volume - Decrease
"pactl set-sink-volume @DEFAULT_SINK@ -2%;pkill -RTMIN+11 dwmblocks"
  Mod2 + XF86AudioLowerVolume

# Title: Speaker Volume - Decrease
"pactl set-sink-volume @DEFAULT_SINK@ -2%;pkill -RTMIN+11 dwmblocks"
  Mod4 + Left

# Title: Speaker Volume - Increase
"pactl set-sink-volume @DEFAULT_SINK@ +2%;pkill -RTMIN+11 dwmblocks"
  Mod2 + XF86AudioRaiseVolume

# Title: Speaker Volume - Increase
"pactl set-sink-volume @DEFAULT_SINK@ +2%;pkill -RTMIN+11 dwmblocks"
  Mod4 + Right

# Title: Mic Volume - Mute
"amixer -q -D pulse sset Capture toggle;pkill -RTMIN+12 dwmblocks"
  Shift + Mod2 + XF86AudioMute

# Title: Mic Volume - Mute
"amixer -q -D pulse sset Capture toggle;pkill -RTMIN+12 dwmblocks"
  Mod4 + Shift + m

# Title: Mic Volume - Decrease
"pactl set-source-volume @DEFAULT_SOURCE@ -2%;pkill -RTMIN+12 dwmblocks"
  Shift + Mod2 + XF86AudioLowerVolume

# Title: Mic Volume - Decrease
"pactl set-source-volume @DEFAULT_SOURCE@ -2%;pkill -RTMIN+12 dwmblocks"
  Mod4 + Shift + Left

# Title: Mic Volume - Increase
"pactl set-source-volume @DEFAULT_SOURCE@ +2%;pkill -RTMIN+12 dwmblocks"
  Shift + Mod2 + XF86AudioRaiseVolume

# Title: Mic Volume - Increase
"pactl set-source-volume @DEFAULT_SOURCE@ +2%;pkill -RTMIN+12 dwmblocks"
  Mod4 + Shift + Right

# Title: Media - Previous track
"playerctl previous"
  Mod2 + XF86AudioPrev

# Title: Media - Play/Pause
"playerctl play-pause"
  Mod2 + XF86AudioPlay

# Title: Media - Next track
"playerctl next"
  Mod2 + XF86AudioNext
